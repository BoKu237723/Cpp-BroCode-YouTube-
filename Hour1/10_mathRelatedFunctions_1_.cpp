#include <iostream>
#include <cmath> //pow //sqrt //celi
int main()
{
    double x = 3.6;
    double y = 4;
    double z;

    // z = std::max(x,y); this shows max

    // z = std::min(x,y); this shows min

    // z = pow(2,3); this shows; power

    // z = sqrt(9); this is squareroot

    //z = abs(-3); //give positive

    //z = ceil(x); rounded

    std::cout << z;
}